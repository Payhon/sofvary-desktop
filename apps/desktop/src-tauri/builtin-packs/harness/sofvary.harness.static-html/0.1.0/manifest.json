{
  "schemaVersion": "1.0",
  "type": "sofvary.harness-pack",
  "id": "sofvary.harness.static-html",
  "version": "0.1.0",
  "name": "Static HTML Harness",
  "description": "Constrains static HTML generation to workspace-local HTML, CSS, and JavaScript files.",
  "compatibility": {
    "client": ">=0.1.0",
    "os": {
      "windows": {
        "supported": true,
        "minVersion": "10.0.17763"
      },
      "macos": {
        "supported": true,
        "minVersion": "12.0"
      },
      "linux": {
        "supported": true
      }
    },
    "arch": ["x64", "arm64"]
  },
  "runtime": "sofvary.runtime.static-html",
  "instructions": {
    "system": [
      "Generate a self-contained static app using index.html, style.css, and app.js.",
      "Do not require npm, CDNs, remote scripts, or external assets."
    ],
    "fileSystemPolicy": [
      "Write only inside generated/static for this runtime.",
      "Do not read or write paths outside the active Sofvary workspace."
    ],
    "outputRules": [
      "Keep Sofvary shell UI out of generated app source.",
      "The app must run through the local static preview server."
    ]
  },
  "integrity": {}
}
