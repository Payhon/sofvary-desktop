# Claude Code Instructions

Use `SOFVARY_AGENT_PROMPT.md` as the primary task prompt.

Generate the app only under `{{runtime.generatedRoot}}`. Keep output within the files listed in `.sofvary/allowed-files.json`.
